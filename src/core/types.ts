export type Domain="Math"|"Reading & Writing"; export type Difficulty=1|2|3|4; export type Confidence="Guess"|"Unsure"|"Pretty Sure"|"Certain"; export type ErrorKind="Didn't know concept"|"Forgot formula"|"Arithmetic"|"Misread"|"Time pressure"|"Second guessed"|"Logic error"|"Other";
export type Representation="equation"|"graph"|"table"|"word-problem"|"diagram"|"real-world"|"passage"; export type Choice={id:string;text:string;reason:string}; export type Question={id:string;templateId:string;conceptId:string;domain:Domain;difficulty:Difficulty;representation?:Representation;prompt:string;kind:"multiple-choice"|"numeric"|"grid-in";choices?:Choice[];answer:string;explanation:string;alternate?:string;desmos?:string;preferredMethod?:"hand"|"calculator"|"either"};
export type CalculatorUsage={opened:boolean;elapsedMs:number}; export type Attempt={id:string;sessionId:string;question:Question;response:string;correct:boolean;elapsedMs:number;confidence:Confidence;errorKind?:ErrorKind;calculator?:CalculatorUsage;at:string}; export type Mastery={conceptId:string;score:number;attempts:number;correct:number;averageMs:number;confidenceCalibration:number;lastReviewed:string;difficultyReached:Difficulty;errors:Partial<Record<ErrorKind,number>>};
export type Session={id:string;startedAt:string;endedAt?:string;target:number|null;attemptIds:string[]};
export type ProbabilisticMastery={conceptId:string;mean:number;variance:number;lower95:number;upper95:number;retention:number;stabilityDays:number;reviewAfter:string;exposures:number;learningVelocity:number;recentTrend:number;historicalTrend:number;difficultyCeiling:Difficulty;representations:Partial<Record<Representation,number>>;updatedAt:string};
