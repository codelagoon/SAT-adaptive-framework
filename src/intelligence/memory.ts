import type {Attempt,ProbabilisticMastery} from "@/core/types";
const DAY=86_400_000, clamp=(x:number,a=0,b=1)=>Math.max(a,Math.min(b,x));
export function retentionAt(state:ProbabilisticMastery,at=new Date()):number{const days=Math.max(0,(at.getTime()-new Date(state.updatedAt).getTime())/DAY);return clamp(Math.exp(-days/Math.max(.25,state.stabilityDays)))}
export function initialBelief(conceptId:string,at=new Date()):ProbabilisticMastery{return {conceptId,mean:.35,variance:.08,lower95:.05,upper95:.65,retention:1,stabilityDays:1,reviewAfter:new Date(at.getTime()+DAY).toISOString(),exposures:0,learningVelocity:0,recentTrend:0,historicalTrend:0,difficultyCeiling:1,representations:{},updatedAt:at.toISOString()}}
export function updateBelief(previous:ProbabilisticMastery|undefined,a:Attempt):ProbabilisticMastery{const prior=previous??initialBelief(a.question.conceptId,new Date(a.at));const retained=prior.mean*retentionAt(prior,new Date(a.at));const confidence={Guess:.25,Unsure:.5,"Pretty Sure":.75,Certain:1}[a.confidence];const expectedTime=110-15*a.question.difficulty,timeWeight=clamp(expectedTime/(a.elapsedMs/1000),.7,1.15);const reliability=.55+.25*confidence;const observation=a.correct?clamp(.78+.05*a.question.difficulty)*timeWeight:clamp(.18-.08*confidence);const gain=clamp(prior.variance/(prior.variance+.12/reliability),.12,.55);const mean=clamp(retained+gain*(observation-retained));const variance=clamp((1-gain)*prior.variance,.012,.12);const sd=Math.sqrt(variance),delta=mean-prior.mean;const stabilityDays=clamp(prior.stabilityDays*(a.correct?1.45+.2*a.question.difficulty:.65),.25,180);const rep=a.question.representation??(a.question.domain==="Math"?"equation":"passage");return {...prior,mean,variance,lower95:clamp(mean-1.96*sd),upper95:clamp(mean+1.96*sd),retention:1,stabilityDays,reviewAfter:new Date(new Date(a.at).getTime()+stabilityDays*DAY*.8).toISOString(),exposures:prior.exposures+1,learningVelocity:.7*prior.learningVelocity+.3*delta,recentTrend:.55*prior.recentTrend+.45*delta,historicalTrend:.9*prior.historicalTrend+.1*delta,difficultyCeiling:Math.max(prior.difficultyCeiling,a.question.difficulty) as ProbabilisticMastery["difficultyCeiling"],representations:{...prior.representations,[rep]:(prior.representations[rep]??0)+1},updatedAt:a.at}}
