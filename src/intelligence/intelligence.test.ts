import {describe,expect,it} from "vitest"; import {makeQuestion} from "@/core/templates"; import {generateValidated,initialBelief,retentionAt,selectCandidate,validateQuestion} from ".";
describe("intelligence engine",()=>{it("validates generated items",()=>{for(let i=0;i<50;i++)expect(validateQuestion(makeQuestion(Math.random,1)).valid).toBe(true)});it("models forgetting monotonically",()=>{const b=initialBelief("x",new Date("2026-01-01"));expect(retentionAt(b,new Date("2026-01-10"))).toBeLessThan(retentionAt(b,new Date("2026-01-02")))});it("returns a scored candidate",()=>{const result=selectCandidate([{templateId:"a",conceptId:"x",representation:"equation",difficulty:1}],{},[],[]);expect(result?.candidate.templateId).toBe("a");expect(result?.score.total).toBeGreaterThan(0)});it("rejects duplicate distractors",()=>{const q=makeQuestion(Math.random,1);if(q.choices){q.choices[1]!.text=q.choices[0]!.text;expect(validateQuestion(q).valid).toBe(false)}});it("regenerates until valid",()=>expect(generateValidated(()=>makeQuestion(Math.random,1))).toBeTruthy())});
