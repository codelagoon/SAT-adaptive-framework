import type {Question} from "@/core/types";
export type Validation={valid:boolean;errors:string[]};
export function validateQuestion(q:Question):Validation{const errors:string[]=[];if(!q.prompt.trim())errors.push("Prompt is empty");if(!q.answer.trim())errors.push("Answer is empty");if(!q.explanation.trim())errors.push("Explanation is empty");if(q.choices){const values=q.choices.map(c=>c.text.trim().toLowerCase());if(new Set(values).size!==values.length)errors.push("Duplicate choices");if(!values.includes(q.answer.trim().toLowerCase()))errors.push("Answer is not among choices");if(q.choices.length!==4)errors.push("SAT multiple choice requires four choices")}if(!q.conceptId||!q.templateId)errors.push("Missing content mapping");return {valid:errors.length===0,errors}}
export function generateValidated<T extends Question>(make:()=>T,maxAttempts=20):T{let last:Validation|undefined;for(let i=0;i<maxAttempts;i++){const q=make();last=validateQuestion(q);if(last.valid)return q}throw new Error(`Question generator failed validation: ${last?.errors.join(", ")}`)}
